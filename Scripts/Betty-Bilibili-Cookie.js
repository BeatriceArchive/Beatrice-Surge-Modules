const NAME="贝蒂的哔哩哔哩 Cookie 获取";const VER="1.4.0";const CK="betty.bilibili.cookie";const META="betty.bilibili.cookie.meta";const BAD="betty.bilibili.cookie.invalid_notice";const LOCK="betty.bilibili.cookie.run_lock";const STATE="betty.bilibili.cookie.panel_state";const GEN="https://passport.bilibili.com/x/passport-login/web/qrcode/generate";const POLL="https://passport.bilibili.com/x/passport-login/web/qrcode/poll";const HOME="https://www.bilibili.com/";const SPI="https://api.bilibili.com/x/frontend/finger/spi";const NAV="https://api.bilibili.com/x/web-interface/nav";const UA="Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/18.0 Mobile/15E148 Safari/604.1";const REQUIRED=["SESSDATA","bili_jct","DedeUserID","buvid3"];const COOKIE_ORDER=["SESSDATA","bili_jct","DedeUserID","DedeUserID__ckMd5","sid","buvid3","buvid4","b_nut","b_lsid","_uuid","buvid_fp"];const SESSION_SCHEMA="official-qr-home-v2";const REQUEST_TIMEOUT=7;const POLL_INTERVAL_MS=3000;const POLL_MAX_MS=165000;const POLL_MAX_ERRORS=3;const LOCK_TTL_MS=210000;let doneCalled=false;let lockOwner="";let lockHeld=false;let panel=readPanelState()||localPanel();main().finally(()=>{releaseLock();savePanelState(panel);finish(panel);});async function main(){try{if(isAutoPanelRefresh()){panel=readPanelState()||localPanel();return;}if(!(await acquireLock())){panel=P("⚠️ 登录工具正在运行｜请完成当前扫码流程","clock.fill","#FF9F0A");return;}resetStoredSession();panel=P("🔄 旧 Cookie 已清除｜正在申请全新官方二维码","arrow.clockwise.circle.fill","#0A84FF");savePanelState(panel);const tx=await createQrTransaction();notifyQr(tx.url);let cookies=await waitForQrLogin(tx.key);const homeResult=await mergeHomeCookies(cookies);cookies=homeResult.cookies;let buvidSource=homeResult.addedBuvid3?"home":"login";if(!cookies.buvid3){const device=await getDeviceCookies(serializeCookies(cookies));if(device.buvid3){Object.assign(cookies,device);buvidSource="spi";}}const missing=REQUIRED.filter((name)=>!cookies[name]);if(missing.length){throw failure("Cookie 不完整","缺少必要字段："+missing.join("、")+"。本次新会话不会保存。","cookie_incomplete");}const cookieHeader=serializeCookies(cookies);const nav=(await getJson(NAV,cookieHeader,true)).body;if(!isLoggedIn(nav)||String(nav.data.mid||"")!==String(cookies.DedeUserID||"")){throw failure("Cookie 验证失败","新会话未通过 Bilibili /nav 登录验证，本次不会保存。","cookie_invalid");}if(!$persistentStore.write(cookieHeader,CK)){throw failure("保存失败","Surge 本地 Cookie 写入失败。","cookie_store_failed");}const meta={version:VER,schema:SESSION_SCHEMA,verified:true,updatedAt:Date.now(),source:"official-web-qr+home",uid:String(cookies.DedeUserID||""),buvid3Source:buvidSource};if(!$persistentStore.write(JSON.stringify(meta),META)){$persistentStore.write("",CK);throw failure("保存失败","会话验证标记写入失败，已回滚清除 Cookie。","meta_store_failed");}$persistentStore.write("",BAD);panel=P("✅ Cookie 已验证并保存｜刷新会清空后重新扫码","key.fill","#34C759");$notification.post(NAME,"✅ Cookie 已验证并保存","官方二维码登录完成，主站会话已补全并通过 /nav 验证。");}catch(error){const safe=normalizeFailure(error);panel=P("❌ "+safe.title+"｜当前无可用新 Cookie","xmark.circle.fill","#FF3B30");console.log("[Betty-Bilibili-Cookie] "+safe.code);$notification.post(NAME,"❌ "+safe.title,safe.body);}}function resetStoredSession(){$persistentStore.write("",CK);$persistentStore.write("",META);$persistentStore.write("",BAD);$persistentStore.write("",STATE);if($persistentStore.read(CK)||$persistentStore.read(META)){throw failure("重置失败","旧 Cookie 或验证标记未能彻底清除，本次不创建登录事务。","reset_failed");}}async function createQrTransaction(){const response=await getJson(GEN,"",false);const body=response.body;if(!body||apiCode(body)!==0||!body.data){throw failure("二维码申请失败","Bilibili 未能创建官方扫码登录事务。","qr_generate_failed");}const key=String(body.data.qrcode_key||"").trim();const url=String(body.data.url||"").trim();if(key.length!==32){throw failure("二维码申请失败","Bilibili 返回的 qrcode_key 长度异常。","qr_key_invalid");}if(!url){throw failure("二维码申请失败","Bilibili 返回的二维码内容为空。","qr_url_empty");}return{key,url};}function notifyQr(url){let image="";try{image=qr64(url);}catch(_){image="";}if(!image){throw failure("二维码生成失败","Bilibili 已返回登录事务，但本机无法编码该二维码内容。","qr_render_failed");}$notification.post(NAME,"二维码已生成｜长按查看","长按通知查看二维码并截图，然后打开 Bilibili → 扫一扫 → 相册，选择截图并确认登录。",{"media-base64":image,"media-base64-mime":"image/png",sound:true});}async function waitForQrLogin(key){const startedAt=Date.now();let consecutiveErrors=0;while(Date.now()-startedAt<POLL_MAX_MS){const url=POLL+"?qrcode_key="+encodeURIComponent(key)+"&source=main_mini";const response=await request(url,"",false,true,true);if(!response.ok){consecutiveErrors+=1;if(consecutiveErrors>=POLL_MAX_ERRORS){throw failure("登录状态查询失败","连续多次无法查询 Bilibili 扫码状态，请重新获取二维码。","qr_poll_network");}await sleep(POLL_INTERVAL_MS);continue;}consecutiveErrors=0;const body=response.body;if(!body||apiCode(body)!==0||!body.data){throw failure("登录状态异常","Bilibili 返回了无法识别的扫码状态。","qr_poll_api");}const status=Number(body.data.code);if(status===0){const map=extractCookies(response.headers,body.data.url);const missing=["SESSDATA","bili_jct","DedeUserID"].filter((name)=>!map[name]);if(missing.length){throw failure("Cookie 不完整","扫码确认成功，但登录响应缺少账号 Cookie。","qr_login_cookie_missing");}return map;}if(status===86038){throw failure("二维码已过期","请重新刷新 Cookie Panel 生成新二维码。","qr_expired");}if(status!==86101&&status!==86090){throw failure("登录状态异常","Bilibili 返回扫码状态码："+safeCode(status)+"。","qr_status_unsupported");}await sleep(POLL_INTERVAL_MS);}throw failure("登录等待超时","未在二维码有效期内完成扫码确认，请重新获取。","qr_timeout");}async function mergeHomeCookies(map){const before=!!map.buvid3;const cookie=serializeCookies(map);const response=await request(HOME,cookie,false,true,false);if(response.headers){Object.assign(map,extractCookies(response.headers,""));}return{cookies:map,addedBuvid3:!before&&!!map.buvid3};}async function getDeviceCookies(cookie){try{const body=(await getJson(SPI,cookie,true)).body;const out={};if(body&&apiCode(body)===0&&body.data){if(safeCookieValue(body.data.b_3))out.buvid3=safeCookieValue(body.data.b_3);if(safeCookieValue(body.data.b_4))out.buvid4=safeCookieValue(body.data.b_4);}return out;}catch(_){return{};}}async function getJson(url,cookie,retry){for(let attempt=0;attempt<=(retry?1:0);attempt+=1){const response=await request(url,cookie,false,true,true);if(response.ok)return response;if(!response.retry||attempt===(retry?1:0))break;await sleep(400*(attempt+1));}throw failure("网络请求失败","无法完成 Bilibili 官方接口请求。","request_failed");}function request(url,cookie,autoRedirect,fullHeaderMode,parseJson){return new Promise((resolve)=>{const options={url,headers:headers(cookie),timeout:REQUEST_TIMEOUT,"auto-cookie":false,"auto-redirect":!!autoRedirect,"full-header-mode":!!fullHeaderMode};$httpClient.get(options,(error,response,data)=>{if(error){resolve({ok:false,retry:true,status:null,headers:null,body:null});return;}const status=response&&Number.isFinite(Number(response.status))?Number(response.status):null;const responseHeaders=response?response.headers:null;let body=data;if(parseJson){try{body=typeof data==="string"?JSON.parse(data):data;}catch(_){body=null;}}const ok=status!==null&&status>=200&&status<400&&(!parseJson||body!==null);resolve({ok,retry:status===null||status>=500,status,headers:responseHeaders,body});});});}function headers(cookie){const out={"User-Agent":UA,Accept:"application/json, text/plain, */*","Accept-Language":"zh-CN,zh-Hans;q=0.9,en;q=0.8",Referer:HOME};if(cookie)out.Cookie=cookie;return out;}function extractCookies(responseHeaders,fallbackUrl){const map={};const add=(raw)=>{const first=String(raw||"").split(";",1)[0];const index=first.indexOf("=");if(index<=0)return;const name=first.slice(0,index).trim();const value=safeCookieValue(first.slice(index+1));if(name&&value)map[name]=value;};if(Array.isArray(responseHeaders)){responseHeaders.forEach((item)=>{if(item&&String(item.field||item.name||"").toLowerCase()==="set-cookie")add(item.value);});}else if(responseHeaders&&typeof responseHeaders==="object"){Object.keys(responseHeaders).forEach((name)=>{if(name.toLowerCase()!=="set-cookie")return;const value=responseHeaders[name];Array.isArray(value)?value.forEach(add):add(value);});}try{const url=String(fallbackUrl||"");const query=url.indexOf("?")>=0?url.slice(url.indexOf("?")+1):"";query.split("&").forEach((part)=>{const index=part.indexOf("=");if(index<=0)return;const name=decodeURIComponent(part.slice(0,index));const value=safeCookieValue(decodeURIComponent(part.slice(index+1)));if(COOKIE_ORDER.indexOf(name)>=0&&value)map[name]=value;});}catch(_){}return map;}function serializeCookies(map){const result=[];const used={};COOKIE_ORDER.forEach((name)=>{if(map[name]){result.push(name+"="+map[name]);used[name]=true;}});Object.keys(map).sort().forEach((name)=>{if(!used[name]&&/^[A-Za-z0-9_.-]{1,64}$/.test(name)&&safeCookieValue(map[name])){result.push(name+"="+map[name]);}});return result.join("; ");}function safeCookieValue(value){const text=String(value||"").trim();return text&&!/[;\r\n]/.test(text)&&text.length<4096?text:"";}function parseCookieHeader(cookie){const map={};String(cookie||"").split(";").forEach((part)=>{const index=part.indexOf("=");if(index>0)map[part.slice(0,index).trim()]=part.slice(index+1);});return map;}function readMeta(){const raw=$persistentStore.read(META);if(!raw)return null;try{const meta=JSON.parse(raw);return meta&&typeof meta==="object"?meta:null;}catch(_){return null;}}function localPanel(){const cookie=$persistentStore.read(CK);const bad=$persistentStore.read(BAD);const meta=readMeta();const parsed=parseCookieHeader(cookie);if(bad)return P("❌ Cookie 已标记失效｜刷新会清空并重新扫码","key.slash.fill","#FF3B30");if(cookie&&REQUIRED.every((name)=>parsed[name])&&meta&&meta.verified===true&&meta.schema===SESSION_SCHEMA){return P("✅ Cookie 已验证并保存｜刷新会清空后重新扫码","key.fill","#34C759");}if(cookie)return P("⚠️ 检测到旧或未验证 Cookie｜刷新会先清空","exclamationmark.triangle.fill","#FF9F0A");return P("点击刷新清空旧状态并生成全新官方二维码","key.fill","#8E8E93");}function readPanelState(){const raw=$persistentStore.read(STATE);if(!raw)return null;try{const value=JSON.parse(raw);return value&&value.title&&value.content?value:null;}catch(_){return null;}}function savePanelState(value){if(!value||!value.title||!value.content)return;$persistentStore.write(JSON.stringify(value),STATE);}function apiCode(body){const value=Number(body&&body.code);return Number.isFinite(value)?value:null;}function isLoggedIn(body){return!!(body&&apiCode(body)===0&&body.data&&body.data.isLogin===true);}function safeCode(value){return Number.isFinite(Number(value))?String(Number(value)):"未知";}function failure(title,body,code){return{__bettyFailure:true,title,body,code};}function normalizeFailure(error){return error&&error.__bettyFailure?error:{title:"执行失败",body:"脚本遇到未预期错误。",code:"unexpected"};}async function acquireLock(){const now=Date.now();const existing=readLock();if(existing&&existing.expiresAt>now)return false;const id=now.toString(36)+"-"+Math.random().toString(36).slice(2,12);if(!$persistentStore.write(JSON.stringify({owner:id,expiresAt:now+LOCK_TTL_MS}),LOCK)){throw failure("运行锁失败","无法创建本地运行锁。","lock_store_failed");}for(let round=0;round<2;round+=1){await sleep(120);const current=readLock();if(!current||current.owner!==id||current.expiresAt<=Date.now())return false;}lockOwner=id;lockHeld=true;return true;}function readLock(){const raw=$persistentStore.read(LOCK);if(!raw)return null;try{const value=JSON.parse(raw);if(!value||!value.owner||!Number.isFinite(Number(value.expiresAt)))return null;return{owner:String(value.owner),expiresAt:Number(value.expiresAt)};}catch(_){return null;}}function releaseLock(){if(!lockHeld)return;const current=readLock();if(current&&current.owner===lockOwner)$persistentStore.write("",LOCK);lockHeld=false;lockOwner="";}function isPanelCall(){return typeof $input==="object"&&$input&&$input.purpose==="panel";}function isAutoPanelRefresh(){return isPanelCall()&&$trigger==="auto-interval";}function P(content,icon,color){return{title:NAME,content,icon,"icon-color":color};}function finish(value){if(doneCalled)return;doneCalled=true;isPanelCall()?$done(value):$done();}function sleep(ms){return new Promise((resolve)=>setTimeout(resolve,ms));}function qr64(text){const data=utf8(text);if(data.length>134)return"";return b64(png(qr(data),8,4));}function qr(data){const n=41,bits=[];pb(bits,4,4);pb(bits,data.length,8);data.forEach(x=>pb(bits,x,8));while(bits.length<1088&&bits.length%8)bits.push(0);const a=[];for(let i=0;i<bits.length;i+=8){let v=0;for(let j=0;j<8;j++)v=v*2+bits[i+j];a.push(v)}for(let i=0;a.length<136;i++)a.push(i%2?17:236);const b=[a.slice(0,68),a.slice(68)],e=b.map(x=>rs(x,18)),cw=[];for(let i=0;i<68;i++)cw.push(b[0][i],b[1][i]);for(let i=0;i<18;i++)cw.push(e[0][i],e[1][i]);const m=Array.from({length:n},()=>Array(n).fill(null));find(m,0,0);find(m,n-7,0);find(m,0,n-7);for(let i=8;i<n-8;i++){if(m[6][i]===null)m[6][i]=i%2===0;if(m[i][6]===null)m[i][6]=i%2===0}for(let y=-2;y<=2;y++)for(let x=-2;x<=2;x++)m[34+y][34+x]=Math.max(Math.abs(x),Math.abs(y))===2||(x===0&&y===0);fmt(m);m[n-8][8]=true;const s=[];cw.forEach(x=>pb(s,x,8));for(let i=0;i<7;i++)s.push(0);let k=0,up=true;for(let c=n-1;c>0;c-=2){if(c===6)c--;for(let z=0;z<n;z++){const r=up?n-1-z:z;for(let j=0;j<2;j++){const x=c-j;if(m[r][x]!==null)continue;let v=k<s.length?s[k++]===1:false;if((r+x)%2===0)v=!v;m[r][x]=v}}up=!up}return m}function find(m,y,x){const n=m.length;for(let a=-1;a<=7;a++)for(let b=-1;b<=7;b++){const r=y+a,c=x+b;if(r<0||c<0||r>=n||c>=n)continue;const q=a>=0&&a<=6&&b>=0&&b<=6;m[r][c]=q&&(a===0||a===6||b===0||b===6||(a>=2&&a<=4&&b>=2&&b<=4))}}function fmt(m){const n=m.length,v=bch(8),A=[[0,8],[1,8],[2,8],[3,8],[4,8],[5,8],[7,8],[8,8],[n-7,8],[n-6,8],[n-5,8],[n-4,8],[n-3,8],[n-2,8],[n-1,8]],B=[[8,n-1],[8,n-2],[8,n-3],[8,n-4],[8,n-5],[8,n-6],[8,n-7],[8,n-8],[8,7],[8,5],[8,4],[8,3],[8,2],[8,1],[8,0]];for(let i=0;i<15;i++){const z=((v>>i)&1)===1;m[A[i][0]][A[i][1]]=z;m[B[i][0]][B[i][1]]=z}}function bch(x){let d=x<<10;while(bl(d)>=11)d^=0x537<<(bl(d)-11);return((x<<10)|d)^0x5412}function bl(x){let n=0;while(x){n++;x>>>=1}return n}function pb(a,v,n){for(let i=n-1;i>=0;i--)a.push((v>>>i)&1)}function rs(data,n){const ex=Array(512),lg=Array(256).fill(0);let x=1;for(let i=0;i<255;i++){ex[i]=x;lg[x]=i;x<<=1;if(x&256)x^=285}for(let i=255;i<512;i++)ex[i]=ex[i-255];const mul=(a,b)=>a&&b?ex[lg[a]+lg[b]]:0;let g=[1];for(let i=0;i<n;i++){const z=Array(g.length+1).fill(0);for(let j=0;j<g.length;j++){z[j]^=g[j];z[j+1]^=mul(g[j],ex[i])}g=z}const m=data.concat(Array(n).fill(0));for(let i=0;i<data.length;i++){const f=m[i];if(f)for(let j=0;j<g.length;j++)m[i+j]^=mul(g[j],f)}return m.slice(data.length)}function utf8(s){const a=[];for(let i=0;i<s.length;i++){let c=s.charCodeAt(i);if(c<128)a.push(c);else if(c<2048)a.push(192|(c>>6),128|(c&63));else if(c>=0xd800&&c<=0xdbff&&i+1<s.length){const l=s.charCodeAt(++i),p=0x10000+((c-0xd800)<<10)+(l-0xdc00);a.push(240|(p>>18),128|((p>>12)&63),128|((p>>6)&63),128|(p&63))}else a.push(224|(c>>12),128|((c>>6)&63),128|(c&63))}return a}function png(m,sc,q){const w=(m.length+q*2)*sc,raw=[];for(let y=0;y<w;y++){raw.push(0);const r=Math.floor(y/sc)-q;for(let x=0;x<w;x++){const c=Math.floor(x/sc)-q;raw.push(r>=0&&c>=0&&r<m.length&&c<m.length&&m[r][c]?0:255)}}const out=[137,80,78,71,13,10,26,10];chunk(out,"IHDR",u32(w).concat(u32(w),[8,0,0,0,0]));chunk(out,"IDAT",zlib(raw));chunk(out,"IEND",[]);return out}function zlib(a){const o=[120,1];let p=0;while(p<a.length){const n=Math.min(65535,a.length-p),f=p+n>=a.length?1:0;o.push(f,n&255,(n>>8)&255,(~n)&255,((~n)>>8)&255);for(let i=0;i<n;i++)o.push(a[p+i]);p+=n}o.push(...u32(adler(a)));return o}function ap(o,a){for(let i=0;i<a.length;i++)o.push(a[i]&255)}function chunk(o,t,d){ap(o,u32(d.length));const a=[...t].map(c=>c.charCodeAt(0));ap(o,a);ap(o,d);ap(o,u32(crc(a.concat(d))))}function u32(v){v>>>=0;return[(v>>>24)&255,(v>>>16)&255,(v>>>8)&255,v&255]}function crc(a){let c=0xffffffff;for(const x of a){c^=x;for(let i=0;i<8;i++)c=(c>>>1)^((c&1)?0xedb88320:0)}return(c^0xffffffff)>>>0}function adler(a){let x=1,y=0;for(const v of a){x=(x+v)%65521;y=(y+x)%65521}return((y<<16)|x)>>>0}function b64(a){const s="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";let o="";for(let i=0;i<a.length;i+=3){const x=a[i],y=i+1<a.length?a[i+1]:0,z=i+2<a.length?a[i+2]:0,v=(x<<16)|(y<<8)|z;o+=s[(v>>>18)&63]+s[(v>>>12)&63]+(i+1<a.length?s[(v>>>6)&63]:"=")+(i+2<a.length?s[v&63]:"=")}return o}
